# Single-port dev: Vite proxies API to backend. User only opens VITE_PORT.
# Leave VITE_SERVER_URL unset (or empty) to use same-origin - no CORS needed.
# VITE_SERVER_URL=

# Vite dev server port (the one port you open in the browser)
VITE_PORT=5173

# Backend URL for Vite proxy (must match server PORT)
VITE_PROXY_TARGET=http://localhost:3000
