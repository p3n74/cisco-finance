{
  "name": "@cisco-finance/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "bun x prisma generate",
    "db:push": "bun x prisma db push",
    "db:generate": "bun x prisma generate",
    "db:migrate": "bun x prisma migrate dev",
    "db:studio": "bun x prisma studio"
  },
  "dependencies": {
    "@cisco-finance/env": "workspace:*",
    "@prisma/client": "catalog:",
    "@prisma/extension-accelerate": "^3.0.1",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cisco-finance/config": "workspace:*",
    "prisma": "^7.2.0",
    "typescript": "catalog:"
  }
}
